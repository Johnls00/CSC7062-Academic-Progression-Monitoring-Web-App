const connection = require("../config/config");

/**
 * This function will gather a student records and return an object with the calculated details
 * @param {*} studentId
 */
async function getStudenRecord(studentId) {
  const studentRecord = {};

  
}

module.exports = {};
